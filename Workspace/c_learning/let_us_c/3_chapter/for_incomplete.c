#include <stdio.h>
main( )
{
   int i ;
   for ( i = 1 ; i <= 10 ; )
   {
         printf ( "%d\n", i ) ;
         i=i+1;
   }
}

// any thing can be done outside hopefully condition checking but ; (semi colon ) must be present
